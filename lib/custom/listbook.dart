import 'package:flutter/cupertino.dart';

class LisBook extends StatelessWidget {
  const LisBook({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 200,
      color: Color.fromARGB(255, 223, 13, 6),
    );
  }
}
